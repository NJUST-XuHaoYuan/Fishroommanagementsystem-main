# 海水生物鱼单微信小程序

这是原鱼单网站的原生微信小程序版本，复用现有后端公开接口：

- `GET /api/public/catalog`
- `GET /api/public/bio-records?stockItemId=...`

## 当前发布配置

- 小程序 AppID：`wxb2bc8f8635f3003b`
- 正式接口：`https://www.marineforest.com.cn`
- `request` 合法域名：`https://www.marineforest.com.cn`
- `downloadFile` 合法域名：`https://www.marineforest.com.cn`、`https://cdn.aquaml.com`
- 开发者工具本地私有配置不提交到 Git；正式预览前应开启合法域名校验。

小程序只读取实时公开接口，不内置真实库存快照。接口不可用时显示加载失败，避免后台隐藏策略变更后，旧库存通过离线数据再次出现。

## 跨对话合并基线（2026-09-08）

小程序改动统一在 `codex/shared-foundation` 集成，验证后同步到 `origin/codex/current-base`。本次已合入 `codex/public-catalog-controls` 截至 `dfde0290c49dd2ed14f7076ec253a48077cd374a` 的全部提交，保留以下两组成果：

- 本对话的品牌无边框 Logo、手写“种一片海 / Grow an Ocean”、四大类区分样式、商品页返回分类左对齐（`2c3ca2a`）。
- 另一对话的维护媒体保存修复（`509a212`）、鱼单显示范围及数量上限（`23b68fc`、`e0392b2`、`480db82`）、照片封面与轻量视频预览（`7355e98`）、异常音轨上传兼容（`dfde029`）。其管理端和后端依赖也一并合入，包括批次收入统计（`e05fb5e`）。

鱼单展示仍由后台“销售管理 → 鱼单管理”统一控制；保留媒体优先排序、售出/出库/损耗自动补位、实时刷新及隐藏数据保护。不要恢复旧离线库存快照或旧商品档案的独立公开开关。

后续在各自工作目录开始修改前，先保存未提交工作，再执行：

```bash
git fetch origin
git merge origin/codex/current-base
git merge-base --is-ancestor origin/codex/current-base HEAD
```

2026-09-07 另一对话记录的小程序 `2026.9.7.1` 审核包对应 `7355e98`，不包含本次新合入的品牌和分类样式。此次源码合并不等于微信正式发布；后续上传必须使用合并后的代码，并另行核实审核与正式发布状态。

## 导入与调试

1. 在微信开发者工具中导入 `wechat-miniprogram` 目录。
2. 选择“公开目录”编译模式查看四个大类及其小类。
3. 点击小类进入商品页，点击商品进入可选个体，再点击个体进入详情页。
4. “小类商品”“可选个体”和“个体详情”编译模式可用于直接调试后三层页面。

## 正式发布前

1. 确认正式 HTTPS 接口、目录接口和维护记录接口可用。
2. 在微信公众平台配置上述 `request` 和 `downloadFile` 合法域名。
3. 在开发者工具中开启合法域名校验并完成真机预览。
4. 上传稳定版本，设置体验版并测试后提交审核。

微信小程序正式环境不支持裸 IP 和 HTTP 图片地址。

## 页面

- `pages/catalog/index`：四个固定大类及其小类目录。
- `pages/products/index`：当前小类的商品搜索与选择。
- `pages/specimens/index`：当前商品的可选库存、编号、单条备注和状态标签，并兼容旧品种链接。
- `pages/detail/index`：个体详情、选鱼码、维护时间轴。

小程序只展示公开目录数据，不读取客户、订单、成本、员工、权限等后台私有信息。

## 库存卡片规则（2026-09-09）

- 编号为管理系统填写的个体编号，使用深蓝色；单条生物备注在编号下方红字展示。公开备注只取生物详情的 `stockItems.notes`，不带入客户、订单或商品的其他备注。
- 取消顶部状态筛选按钮；卡片保留状态标签和到货日期，不显示缸位。缸位仍可在详情中查看。
- 有编号或单条备注的个体独立展示。只有未编号、无备注且全部分组条件和完整维护记录一致的个体才合并为“同款可选 N 条”，价格按每条标注。
- 旧接口未提供备注字段时不合并，防止无法确认是否有独立备注的个体被合并。数据库记录和每条个体的选鱼码不变。
